# ====================================
# Backend Server Configuration
# ====================================

# Server Configuration
# Port for the backend API server (default: 3000)
PORT=3000

# CoinGecko API Configuration
# Base URL for CoinGecko API (no API key required for free tier)
# Free tier: 10-30 calls/minute
COINGECKO_API_BASE=https://api.coingecko.com/api/v3

# Price Fetching Configuration
# How often to fetch prices from CoinGecko (in minutes)
# Recommended: 5-10 minutes to avoid rate limiting
FETCH_INTERVAL=5

# Database Configuration
# Path to SQLite database file (relative to crypto-backend directory)
# Will be created automatically if it doesn't exist
DB_PATH=./database.json

# Logging Configuration
# Log level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info
